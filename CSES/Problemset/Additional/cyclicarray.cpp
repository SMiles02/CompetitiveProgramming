#include <bits/stdc++.h>
using namespace std;

const int N = 2e5 + 1, L = 21;
int up[(N + L) * 2][L];

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);
    int n, ep;
    ll k, cur = 0;
    cin >> n >> k;
    int a[n];
    for (int i = 0; i < n; ++i)
        cin >> a[i];
    for (int i = n - 1; i >= 0; --i) {
        if ()
    }
    return 0;
}