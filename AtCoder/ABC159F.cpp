#include <bits/stdc++.h>
#define ll long long
#define sz(x) (int)(x).size()
using namespace std;

int mod=998244353;

int main()
{
    ios_base::sync_with_stdio(0); cin.tie(0);
    int n,s;
    cin>>n>>s;
    int a[n];
    for (int i=0;i<n;++i)
    {
        cin>>a[i];
    }
    ll dp[s+1][n]
    return 0;
}