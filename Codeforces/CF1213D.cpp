#include <bits/stdc++.h>
#define ll long long
#define sz(x) (int)(x).size()
using namespace std;

int main()
{
	ios_base::sync_with_stdio(0); cin.tie(0);
	int n,k,x,f;
	cin>>n>>k;
	map<int,vector<int>> turnTo;
	vector<int> v;
	map<int,int> requiredOperations;
	map<int,int> doneNums;
	for (int i=0;i<n;++i)
	{
		cin>>x;
		v.push_back(x);
		re
		f=1;
		while (x>0)
		{
			v.push_back(x);
		}
	}
	return 0;
}