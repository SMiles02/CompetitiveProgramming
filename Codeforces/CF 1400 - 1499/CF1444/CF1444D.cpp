#include <bits/stdc++.h>
#define ll long long
#define sz(x) (int)(x).size()
using namespace std;

mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());
bitset<1000> b1,b2,n1,n2;
int h,v;

buildFirst()
{

}

int main()
{
    int start = clock();
    ios_base::sync_with_stdio(0); cin.tie(0);
    int n;
    cin>>n;
    while ((clock()-start)/double(CLOCKS_PER_SEC)<0.9&&s1>0)
    {

    }

    return 0;
}