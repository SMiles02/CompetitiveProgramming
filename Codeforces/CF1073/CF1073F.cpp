#include <bits/stdc++.h>
#define ll long long
#define sz(x) (int)(x).size()
using namespace std;

map<int,vector<int>> edges;
bool done[200001];

pair<pair<int,int>,int> dfs(int cur)
{
    done[cur]=1;
    vector<pair<pair<int,int>,int>> v;
    
}

int main()
{
    ios_base::sync_with_stdio(0); cin.tie(0);
    int n;
    cin>>n;
    return 0;
}